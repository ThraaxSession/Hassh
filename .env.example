# Example configuration for Hassh

# Home Assistant URL (use your Nabu Casa remote URL or local URL)
# Example: https://your-instance.ui.nabu.casa
HOME_ASSISTANT_URL=

# Long-lived access token from Home Assistant (optional - users can provide during login)
# Get this from your Home Assistant profile page
HA_TOKEN=

# Host to bind the server to (default: 0.0.0.0 - all interfaces)
# Examples:
#   HOST=0.0.0.0          # Listen on all interfaces (default)
#   HOST=localhost        # Listen on localhost only
#   HOST=192.168.1.10     # Listen on specific IP
#   HOST=myserver.local   # Listen on specific hostname
HOST=0.0.0.0

# Port to run the server on (default: 8080)
PORT=8080

# Entity refresh interval in seconds (default: 30)
REFRESH_INTERVAL=30

# Database file path (default: hassh.db)
DB_PATH=hassh.db

# JWT secret for authentication (auto-generated if not provided)
JWT_SECRET=
